import { answerCollection, db } from "../name";


export default async function createanswerCollection() {
    //create collection 
    await databases.createanswerCollection(db, answerCollection, )
}